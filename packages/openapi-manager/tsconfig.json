{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "downlevelIteration": true,
    // 모듈 해결의 기준 디렉토리
    "baseUrl": ".",
    // 소스 파일의 루트 디렉토리
    "rootDir": "src",
    // 컴파일된 파일 출력 디렉토리 (tsc-alias가 이 경로를 사용)
    "outDir": "dist",
    "paths": {
      "@/*": ["src/*"],
      "@/glam/*": ["src/services/glam/*"],
      "@core/*": ["src/core/*"],
      "@modules/*": ["src/modules/*"],
      "@services/*": ["src/services/*"],
      "@glam/*": ["src/services/glam/*"],
      "@hooks/*": ["src/hooks/*"]
    }
  },
  // 컴파일할 파일 패턴
  "include": ["src/**/*.ts", "src/**/*.tsx"],
  // 제외할 디렉토리/파일
  "exclude": ["dist", "node_modules", "src/services/glam/generated/**/*.ts"]
}
