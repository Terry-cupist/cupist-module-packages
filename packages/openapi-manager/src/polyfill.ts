/**
 * React Native Polyfill Setup
 *
 * Provides Web API globals required by auto-generated OpenAPI code:
 * - fetch, Headers, Request, Response
 * - FormData, URLSearchParams, Blob
 * - TextEncoder, TextDecoder
 * - ReadableStream, TransformStream
 */
import "react-native-polyfill-globals/auto";

export {};
