{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // 모듈 해결의 기준 디렉토리
    "baseUrl": ".",
    // 소스 파일의 루트 디렉토리
    "rootDir": "src"
  },
  // 컴파일할 파일 패턴
  "include": ["src/**/*.ts", "src/**/*.tsx"],
  // 제외할 디렉토리/파일
  "exclude": ["dist", "node_modules"]
}
