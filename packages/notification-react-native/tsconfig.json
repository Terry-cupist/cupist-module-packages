{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // 모듈 해결의 기준 디렉토리
    "baseUrl": ".",
    // 소스 파일의 루트 디렉토리
    "rootDir": "src",
    // 경로 별칭 매핑 (babel alias와 동일하게 설정)
    "paths": {
      "@/*": ["./src/*"],
      "@app/*": ["./src/app/*"],
      "@shared/*": ["./src/shared/*"]
    }
  },
  // 컴파일할 파일 패턴
  "include": ["src/**/*.ts", "src/**/*.tsx"],
  // 제외할 디렉토리/파일
  "exclude": ["dist", "node_modules"]
}
